{
  "name": "@stackpilot/cert-helper",
  "private": true,
  "version": "0.3.0-preview.36",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc",
    "lint": "eslint src",
    "test": "npm run build && node --test ../../tests/cert-helper/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^5.9.3"
  }
}
